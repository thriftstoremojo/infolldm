(function() {
	'use strict';

	describe('infoApp.newsList module', function() {
		beforeEach(module('infoApp.newsList'));

		describe('newsList controller', function(){
			it('should ....', inject(function($controller) {
				//spec body
				var newsListController = $controller('NewsListController');
				expect(newsListController).toBeDefined();
			}));
		});
	});
})();