(function() {
	'use strict';

	describe('infoApp.newsDetail module', function() {
		beforeEach(module('infoApp.newsDetail'));

		describe('newsDetail controller', function(){
			it('should ....', inject(function($controller) {
				//spec body
				var newsDetailController = $controller('NewsDetailController');
				expect(newsDetailController).toBeDefined();
			}));
		});
	});
})();