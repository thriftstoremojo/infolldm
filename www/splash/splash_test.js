(function() {
	'use strict';

	describe('infoApp.splash module', function() {

	  beforeEach(module('infoApp.splash'));

	  describe('splash controller', function(){

	    it('should ....', inject(function($controller) {
	      //spec body
	      var splashController = $controller('SplashController');
	      expect(splashController).toBeDefined();
	    }));

	  });
	});
})();